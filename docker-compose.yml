version: '3'
services:
  app:
    container_name: morellis-app
    build:
      context: .
      args:
        SSH_KEY: ${SSH_KEY}
    command: ["go", "run", "main.go"]
    volumes:
      - .:/go/src/app
    ports:
      - "8080:8080"
